import { Button } from "@/components/ui/button";

export default function Home() {
  return (
    <div className="flex flex-col items-center justify-start pt-32 h-full">
      <Button variant="default" size="lg">Hello World</Button>
    </div>
  );
}
